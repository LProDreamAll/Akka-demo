

/**
  * Copyright (C), 2019-2019
  * FileName: DeviceTest
  * Author:   s·D·bs 
  * Date:     2019/5/25 15:04
  * Description: ${DESCRIPTION}
  * Motto: 0.45%
  *
  */

import org.junit.Test

object DeviceTest {


  @Test
  def testReplyWithEmptyReadingIfNoTemperatureIsKnown(): Unit = {
    //    val system = ActorSystem("DeviceTest-system")
    //    val probe = new Nothing(system)
    //    val deviceActor = system.actorOf(Device.props("group", "device"))
    //    deviceActor.tell(new Device.ReadTemperature(42L), probe.getRef)
    //    val response = probe.expectMsgClass(classOf[Device.RespondTemperature])
    //    assertEquals(42L, response.requestId)
    //    assertEquals(Optional.empty, response.value)
  }


}
